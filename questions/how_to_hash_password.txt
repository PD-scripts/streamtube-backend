//jwt= json web token



import jwt from "jsonwebtoken"
import bcrypt from "bcrypt"


//pre save hook for hashing the password before saving the user to the database



//ab dekho userscema ek model hai jo data ko db me store kr sakta hai , hum chahate hai ki store krne se pehle ek chota sa kuch kaam ho jaye data k sath fir vo store ho ,therefore hum pre hook ka use krte hai , 

//pre hook ek middleware hai jo userschema k data ko manipulate karega before saving to db

//kya manipulate? like hashing the passowrd before saving to db


EVENT  can be validate, save,remove,updateOne,deleteOne


arrow function nhi likhna , direct function likhna in callback kyonlki arrow function me this ka reference nhi milta


userSchema.pre(EVENT, async function (next) {
    if(!this.isModified("password")) return next();// agr password chod k kuch or modify hua to "pre" ka use nhi krna

    this.password = await bcrypt.hash(this.password, 10)
    next()
})

userSchema.methods.isPasswordCorrect = async function(password){
    return await bcrypt.compare(password, this.password)
}


